import { DataSource, Repository } from '@n8n/typeorm';
export declare class UsageMetrics {
}
export declare class UsageMetricsRepository extends Repository<UsageMetrics> {
    constructor(dataSource: DataSource);
    toTableName(name: string): string;
    getLicenseRenewalMetrics(): Promise<{
        enabledUsers: number;
        totalUsers: number;
        activeWorkflows: number;
        totalWorkflows: number;
        totalCredentials: number;
        productionExecutions: number;
        manualExecutions: number;
    }>;
}
