import { ExecutionMetadataRepository } from '../databases/repositories/executionMetadata.repository';
import type { ExecutionMetadata } from '../databases/entities/ExecutionMetadata';
export declare class ExecutionMetadataService {
    private readonly executionMetadataRepository;
    constructor(executionMetadataRepository: ExecutionMetadataRepository);
    save(executionId: string, executionMetadata: Record<string, string>): Promise<ExecutionMetadata[]>;
}
