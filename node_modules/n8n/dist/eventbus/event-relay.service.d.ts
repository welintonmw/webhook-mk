/// <reference types="node" />
import { EventEmitter } from 'node:events';
import type { Event } from './event.types';
export declare class EventRelay extends EventEmitter {
    emit<K extends keyof Event>(eventName: K, arg: Event[K]): boolean;
    on<K extends keyof Event>(eventName: K, handler: (arg: Event[K]) => void): this;
}
