import type { IExecuteFunctions, INodeType, INodeTypeDescription, SupplyData, ILoadOptionsFunctions } from 'n8n-workflow';
export declare class LmOpenAi implements INodeType {
    description: INodeTypeDescription;
    methods: {
        listSearch: {
            openAiModelSearch(this: ILoadOptionsFunctions): Promise<{
                results: {
                    name: string;
                    value: string;
                }[];
            }>;
        };
    };
    supplyData(this: IExecuteFunctions, itemIndex: number): Promise<SupplyData>;
}
