"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.hasScope = void 0;
const combineScopes_1 = require("./combineScopes");
function hasScope(scope, userScopes, masks, options = { mode: 'oneOf' }) {
    if (!Array.isArray(scope)) {
        scope = [scope];
    }
    const userScopeSet = (0, combineScopes_1.combineScopes)(userScopes, masks);
    if (options.mode === 'allOf') {
        return !!scope.length && scope.every((s) => userScopeSet.has(s));
    }
    return scope.some((s) => userScopeSet.has(s));
}
exports.hasScope = hasScope;
//# sourceMappingURL=hasScope.js.map