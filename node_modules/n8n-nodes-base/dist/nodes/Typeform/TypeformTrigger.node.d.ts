import { IHookFunctions, IWebhookFunctions } from 'n8n-core';
import { ICredentialsDecrypted, ICredentialTestFunctions, INodeType, INodeTypeDescription, IWebhookResponseData, NodeCredentialTestResult } from 'n8n-workflow';
import { getForms } from './GenericFunctions';
export declare class TypeformTrigger implements INodeType {
    description: INodeTypeDescription;
    methods: {
        loadOptions: {
            getForms: typeof getForms;
        };
        credentialTest: {
            testTypeformTokenAuth(this: ICredentialTestFunctions, credential: ICredentialsDecrypted): Promise<NodeCredentialTestResult>;
        };
    };
    webhookMethods: {
        default: {
            checkExists(this: IHookFunctions): Promise<boolean>;
            create(this: IHookFunctions): Promise<boolean>;
            delete(this: IHookFunctions): Promise<boolean>;
        };
    };
    webhook(this: IWebhookFunctions): Promise<IWebhookResponseData>;
}
