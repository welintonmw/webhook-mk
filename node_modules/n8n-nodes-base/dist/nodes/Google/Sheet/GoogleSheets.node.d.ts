import { IExecuteFunctions } from 'n8n-core';
import { ICredentialsDecrypted, ICredentialTestFunctions, ILoadOptionsFunctions, INodeExecutionData, INodePropertyOptions, INodeType, INodeTypeDescription, NodeCredentialTestResult } from 'n8n-workflow';
export declare class GoogleSheets implements INodeType {
    description: INodeTypeDescription;
    methods: {
        loadOptions: {
            getSheets(this: ILoadOptionsFunctions): Promise<INodePropertyOptions[]>;
        };
        credentialTest: {
            googleApiCredentialTest(this: ICredentialTestFunctions, credential: ICredentialsDecrypted): Promise<NodeCredentialTestResult>;
        };
    };
    execute(this: IExecuteFunctions): Promise<INodeExecutionData[][]>;
}
