import { IExecuteFunctions } from 'n8n-core';
import { ICredentialsDecrypted, ICredentialTestFunctions, INodeExecutionData, INodeType, INodeTypeDescription, NodeCredentialTestResult } from 'n8n-workflow';
export declare class HomeAssistant implements INodeType {
    description: INodeTypeDescription;
    methods: {
        credentialTest: {
            homeAssistantApiTest(this: ICredentialTestFunctions, credential: ICredentialsDecrypted): Promise<NodeCredentialTestResult>;
        };
    };
    execute(this: IExecuteFunctions): Promise<INodeExecutionData[][]>;
}
