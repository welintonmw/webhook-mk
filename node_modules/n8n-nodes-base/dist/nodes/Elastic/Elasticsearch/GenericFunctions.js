"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.elasticsearchApiRequest = void 0;
const n8n_workflow_1 = require("n8n-workflow");
async function elasticsearchApiRequest(method, endpoint, body = {}, qs = {}) {
    const { username, password, baseUrl, } = await this.getCredentials('elasticsearchApi');
    const token = Buffer.from(`${username}:${password}`).toString('base64');
    const options = {
        headers: {
            Authorization: `Basic ${token}`,
            'Content-Type': 'application/json',
        },
        method,
        body,
        qs,
        uri: `${baseUrl}${endpoint}`,
        json: true,
    };
    if (!Object.keys(body).length) {
        delete options.body;
    }
    if (!Object.keys(qs).length) {
        delete options.qs;
    }
    try {
        return await this.helpers.request(options);
    }
    catch (error) {
        throw new n8n_workflow_1.NodeApiError(this.getNode(), error);
    }
}
exports.elasticsearchApiRequest = elasticsearchApiRequest;
//# sourceMappingURL=GenericFunctions.js.map