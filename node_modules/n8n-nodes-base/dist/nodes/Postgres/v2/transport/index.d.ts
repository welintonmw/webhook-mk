import { Client } from 'ssh2';
import pgPromise from 'pg-promise';
import type { PgpDatabase, PostgresNodeCredentials, PostgresNodeOptions } from '../helpers/interfaces';
export declare function configurePostgres(credentials: PostgresNodeCredentials, options?: PostgresNodeOptions, createdSshClient?: Client): Promise<{
    db: pgPromise.IDatabase<{}, import("pg-promise/typescript/pg-subset").IClient>;
    pgp: pgPromise.IMain<{}, import("pg-promise/typescript/pg-subset").IClient>;
    sshClient?: undefined;
} | {
    db: PgpDatabase;
    pgp: pgPromise.IMain<{}, import("pg-promise/typescript/pg-subset").IClient>;
    sshClient: Client;
}>;
