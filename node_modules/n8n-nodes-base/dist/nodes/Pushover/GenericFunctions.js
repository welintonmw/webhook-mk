"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.pushoverApiRequest = void 0;
const n8n_workflow_1 = require("n8n-workflow");
async function pushoverApiRequest(method, path, body = {}, qs = {}, option = {}) {
    const credentials = await this.getCredentials('pushoverApi');
    if (method === 'GET') {
        qs.token = credentials.apiKey;
    }
    else {
        body.token = credentials.apiKey;
    }
    const options = {
        method,
        formData: body,
        qs,
        uri: `https://api.pushover.net/1${path}`,
        json: true,
    };
    try {
        if (Object.keys(body).length === 0) {
            delete options.body;
        }
        return await this.helpers.request.call(this, options);
    }
    catch (error) {
        throw new n8n_workflow_1.NodeApiError(this.getNode(), error);
    }
}
exports.pushoverApiRequest = pushoverApiRequest;
//# sourceMappingURL=GenericFunctions.js.map